package tap.services.feedback

import akka.actor.{Actor, ActorLogging, Props}

/**
  * Created by andrew@andrewresearch.net on 24/10/16.
  */

/*
class FeedbackService extends Actor with ActorLogging {

  val writingFeedback = context.actorOf(Props[WritingFeedback],"writingFeedback")
  //val writingFeedbackDebug = context.actorOf(Props[WritingFeedbackDebug],"writingFeedbackDebug")

  val documentFeedback = context.actorOf(Props[DocumentFeedback],"documentFeedback")
  val paragraphFeedback = context.actorOf(Props[ParagraphFeedback],"paragraphFeedback")
  val sentenceFeedback = context.actorOf(Props[SentenceFeedback],"sentenceFeedback")
  val expressionFeedback = context.actorOf(Props[ExpressionFeedback],"expressionFeedback")


  override def receive = {
    case _ => log.error("Unknown message received")
  }


}
*/
